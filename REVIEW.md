# مراجعة شاملة للتطبيق — Selim Finance

مراجعة لملف `index.html` (تطبيق صفحة واحدة، ‏~6,400 سطر، React + Firebase، يُترجَم بـ Babel في المتصفح).
النسخة وقت المراجعة: `7.32` → صارت `7.36` بعد أربع جولات من الإصلاحات.

**كل البنود المفتوحة اتقفلت ما عدا نشر القواعد — وده محتاج إيدك انت.**

> **بنية المشروع اتغيّرت**: الكود بقى في `app.src.jsx` و`index.html` بقى قشرة تحميل.
> اقرأ `README.md` قبل أي تعديل.

---

## أولاً: إصلاحات طُبِّقت فعلاً في هذا الفرع

### 1. الشركات المشتركة كانت ممكن تختفي تمامًا عن الشخص المشتَرَك معاه — (خطأ وظيفي حقيقي)
`shareAccess` كان بيخزّن البريد بحروف صغيرة (`.toLowerCase()`)، لكن الاستعلامات والتحقق من الصلاحية
كانت بتقارن بـ `user.email` كما يرجّعه فايربيز — واللي بيحتفظ بشكل البريد وقت التسجيل.
فمستخدم سجّل بـ `Ahmed@Gmail.com` ميلاقيش الشركة المشتركة معاه أبدًا، ولا رسالة خطأ ولا أي مؤشر.

**الإصلاح:** إضافة `normEmail()` و`emailVariants()`، وتوحيد المقارنة في `getRole`، وتحويل
الاستعلامين لـ `array-contains-any` بالشكلين (الموحَّد والأصلي) عشان المستندات القديمة اللي
اتخزن فيها بريد بحروف كبيرة تفضل شغالة كمان.

### 2. أرقام كانت بتتحوّل صفر بصمت عند قراءة الإكسل — (فقد بيانات)
`parseAmountCell` و`parseSignedNum` كانا بيعملا `parseFloat` بعد إزالة الفواصل فقط. النتيجة:

| الشكل في الملف | قبل | بعد |
|---|---|---|
| `(1,234.50)` سالب بالأقواس (شائع جدًا في تقارير المحاسبة) | `0` | `-1234.50` |
| `١٢٣٤٫٥٠` أرقام هندية | `0` | `1234.50` |
| `1,234-` سالب لاحق (تصدير بعض البرامج) | `0` | `-1234` |

كل حالة من دول كانت بتخرج صفر من غير تحذير، يعني ميزان يظهر متزن وهو ناقص بنود.
**الإصلاح:** دالتان جديدتان `normalizeDigits()` و`numericFromCell()` تتعاملان مع الأرقام
الهندية (العربية والفارسية)، الفاصلة العشرية العربية `٫`، فاصلة الآلاف `٬`، المسافات
غير القابلة للكسر، الأقواس، والسالب اللاحق. مُختبَرة على الحالات أعلاه.

### 3. تعقيد تربيعي في قلب حساب القوائم — (أداء)
`computeFigures` كان فيه `srcRows.find(x => x.name === name)` مستدعاة من جوّه فلاتر
بتلف هي كمان على كل الحسابات (9 مواضع). ميزان بـ 3,000 حساب = ملايين المقارنات لكل استدعاء.
**الإصلاح:** فهرس `Map` بالاسم يُبنى مرة واحدة. الدلالة محفوظة بالضبط (أول حساب لكل اسم، زي `find`).

### 4. الذاكرة المؤقتة كانت معطّلة فعليًا — (أداء)
- `sortedPeriods` كانت `[...periods].sort()` بدون `useMemo` → مصفوفة جديدة كل رندر →
  كل `useMemo` معتمدة عليها (أرقام الفترة، السنة، المركز المالي) كانت بتُعاد من الصفر.
- `HomeDashboard` كانت بتحسب 12 شهر + إجمالي السنة في كل رندر، وكل شهر بيستدعي
  `computeFigures` حتى 3 مرات → ‏**~72 مرور كامل على الميزان مع كل ضغطة زر**.

**الإصلاح:** `useMemo` للاثنين. مجتمعة مع البند (3)، دي أكبر مكسب أداء في التطبيق.

### 5. تثبيت إصدارات المكتبات الخارجية — (مخاطرة توافر)
`<script src="https://unpkg.com/@babel/standalone/babel.min.js">` بدون إصدار.
‏`@babel/standalone` وصل فعلاً للإصدار **8.0.4** (نقلة رئيسية)، يعني كل المستخدمين
انتقلوا لإصدار كبير جديد من غير أي اختبار. اختبرت مصدر التطبيق على بابل 8 وهو **بيترجم
تمام حاليًا** — يعني ما فيش عطل قائم دلوقتي، لكن التطبيق كان معلّق على الحظ:
أي تغيير كاسر في بابل بيوقّف التطبيق كله لكل المستخدمين في نفس اللحظة.

**الإصلاح:** تثبيت `@babel/standalone@7.29.8`، و`react`/`react-dom` على `18.3.1` بدل `@18`.

**التحقق:** مصدر التطبيق كامل يُترجَم ويُحلَّل بنجاح بالإصدار المثبّت (اختُبر ببابل 7.29.8 و8.0.4).

---

## ثانيًا: إصلاحات الجولة الثانية

### 6. قواعد أمان Firestore — أُضيف `firestore.rules`
كان أخطر بند في المراجعة: مفيش قواعد في المستودع، وكل التحكم في الصلاحيات على
جهة العميل (`getRole`/`readOnly` بيتحكموا في شكل الواجهة بس).
الملف الجديد بيطبّق نفس نموذج التطبيق على مستوى السيرفر:

- **المالك**: قراءة + كتابة + حذف + إدارة المشاركة
- **المحرّر**: قراءة + كتابة على البيانات، وممنوع صراحةً يغيّر `ownerUid` أو
  يضيف نفسه أو أي حد لقوائم المشاركة
- **المشاهد**: قراءة فقط
- `settings/classmap_{uid}`: كل مستخدم يوصل لمستنده هو بس
- أي مسار تاني: مرفوض افتراضيًا

المقارنة بتتم بالشكلين (الأصلي والموحّد) للبريد، مطابقةً لسلوك التطبيق.

> ⚠ **الملف ده مش بيتنشر لوحده.** لازم `firebase deploy --only firestore:rules`
> أو لصقه في الكونسول. الخطوات في `README.md`.

### 7. ثغرة كشفتها كتابة القواعد: التسجيل من غير تحقق من البريد
التطبيق بيستخدم `createUserWithEmailAndPassword` من غير أي `sendEmailVerification`.
يعني أي حد يقدر يعمل حساب ببريد مش بتاعه، ولو شركة متشاركة مع البريد ده يبقى دخل عليها.

كنت هحط `request.auth.token.email_verified == true` في القواعد، لكن ده كان هيقفل
التطبيق على **كل** المستخدمين الحاليين لأن مفيش ولا واحد مفعّل بريده. فسبته معلّقًا
وموثّقًا في الملف. **ده لسه مفتوح ومحتاج قرارك** (البند في «ثالثًا»).

### 8. مفتاح الحساب بقى الكود بدل الاسم
كل البحث والتجميع كان بمفتاح اسم الحساب. حسابين بنفس الاسم في فرعين مختلفين
كانوا بياخدوا تصنيف الحساب الأول وأرقامهم بتتجمّع في بند واحد.
المفتاح دلوقتي `code` والاسم احتياطي لو الميزان من غير أكواد — في:
`computeFigures` (الفهرس وكل عمليات البحث) و`mergeFigures` (التجميع السنوي).

كمان في `bucketRows` كان فيه بحث بالاسم عن صف **هو نفسه** أصلاً — ممكن يرجّع
حساب تاني بنفس الاسم. اتشال البحث خالص.

> ⚠ **متوقّع تغيّر أرقام** في الشركات اللي مواز��نها فيها أسماء حسابات متكررة.
> الأرقام الجديدة هي الصح، بس هتبان مختلفة عن اللي كنت شايفه. راجع شركة أو
> اتنين قبل ما تعتمد النتائج.

### 9. Service Worker — التطبيق بقى يفتح من غير إنترنت
`sw.js` جديد، بيخزّن قشرة التطبيق **والمكتبات المثبّتة** من أول زيارة.
الاستراتيجية شبكة-أولاً لملفات التطبيق (عشان محدش يفضل على كود قديم فيه خطأ
حسابي اتصلّح)، وcache-first للمكتبات المثبّتة (روابطها ثابتة ومبتتغيّرش).
طلبات فايربيز الحية مستثناة عن قصد عشان محدش يشوف بيانات قديمة.

`forceUpdate()` بقى **يلغي تسجيل الـ SW** كمان، مش يمسح الكاش وبس — من غير كده
الـ SW القديم كان هيرجّع نفس النسخة تاني بعد إعادة التحميل.

**التحقق:** في متصفح حقيقي (Chromium): الـ SW اتسجّل ونشط، خزّن القشرة والمكتبات،
وبعد **قطع الشبكة تمامًا** التطبيق أعاد التحميل ورندر شاشة الدخول من غير أي خطأ.

### 10. خطوة بناء — بابل خرج من متصفح المستخدم
الكود اتنقل لـ `app.src.jsx`، و`npm run build` بيولّد `app.js` جاهز.
`index.html` بقى 2 كيلوبايت بدل 490.

اللي اتشال من تحميل كل مستخدم: `@babel/standalone` (~3 ميجا) + ترجمة 490 كيلوبايت
مصدر على الخيط الرئيسي **مع كل فتحة للتطبيق**.

السكربت كمان بيوزّع `APP_VERSION` تلقائيًا على `index.html` و`sw.js`، فرقم النسخة
بقى في مكان واحد.

**التحقق:** التطبيق المبني اشتغل ورندر شاشة الدخول من غير أي خطأ في الكونسول.

### 11. `periodCount` مبقاش بيتكتب من المشاهدين
كان كل من يفتح الشركة بيحاول يكتب الملخّص، بما فيهم المشاهدين اللي ملهمش صلاحية
كتابة — أخطاء متكررة مبلوعة بـ `.catch(() => {})`. بقى محكوم بصلاحية الكتابة.
(ده كان هيبقى خطأ ظاهر بعد نشر القواعد الجديدة.)

---

## ثالثًا: إصلاحات الجولة الثالثة

### 12. تفعيل البريد الإلكتروني
`sendEmailVerification()` بقت بتتبعت تلقائيًا عند إنشاء أي حساب جديد، وفيه شريط
تنبيه واضح جوّه التطبيق لأي مستخدم بريده مش مفعّل، مع زر لإعادة الإرسال وزر
«فعّلته خلاص» بيعيد تحميل حالة الحساب.

**مقصود إن الدخول مش مقفول على غير المفعّلين** — قفله دلوقتي معناه إن كل
المستخدمين الحاليين يتقفل عليهم فجأة. الخطة: سيبه شغال كده لحد ما الحسابات
الموجودة تفعّل بريدها، وبعدين شيل التعليق عن السطر ده في `firestore.rules`:

```
&& request.auth.token.email_verified == true
```

وساعتها القفل بيبقى على مستوى السيرفر مش الواجهة بس.

**التحقق (Chromium):** الشريط ظهر لمستخدم غير مفعّل، زر الإرسال استدعى
`sendEmailVerification` فعلاً وظهر تأكيد الإرسال.

### 13. فشل الحفظ بقى ظاهر للمستخدم
كانت `catch(() => {})` على عمليات حفظ حقيقية: لو الحفظ فشل (صلاحية، شبكة، حد
الدفعة) المستخدم بيشوف واجهة طبيعية ويفتكر إن البيانات اتحفظت.

اتضافت قناة `reportSaveError` وشريط `SaveErrorBanner` بيقول **إيه اللي فشل**
و**رسالة الخطأ**، وبينبّه إن التعديل على الشاشة بس ومش متسجّل على السيرفر.
اتوصّلت بيها: خريطة التصنيف، إعدادات الشركة، بيانات الزكاة، حذف الفترات
المكرّرة، ودفعات تصنيف الحسابات.

> ده هيبقى مهم بالذات بعد نشر القواعد الجديدة — أي عملية مرفوضة كانت هتفشل بصمت.

**التحقق (Chromium):** الشريط ظهر برسالة `PERMISSION_DENIED` كاملة، وزر الإخفاء شغال.

### 14. معرّفات المستندات
`uid()` كانت `Math.random().toString(36).slice(2,10)` — 8 محارف مش مضمونة
التفرّد، وتصادم واحد معناه فترة بتكتب فوق فترة تانية.
بقت `crypto.randomUUID()` مع بديل على `getRandomValues`.

**التحقق:** بترجّع UUID سليم في المتصفح.

---

## رابعًا: إصلاحات الجولة الرابعة

### 15. اختبارات — 29 اختبار على دوال الحساب
كان أكبر خطر طويل المدى: تطبيق بيطلع قوائم مالية وزكاة من غير أي اختبار.

دوال الحساب اتفصلت في `src/calc.js` (كود خالص من غير React ولا DOM)،
واتغطّت بـ 29 اختبار في `tests/`:

- **قراءة الأرقام**: الأرقام الهندية والفارسية، الفواصل العربية، السالب بالأقواس
  والسالب اللاحق، وطبيعة الرصيد.
- **التصنيف**: توحيد الإملاء، التخمين من طبيعة الرصيد، توزيع حقوق الملكية.
- **القوائم**: قائمة الدخل والمركز المالي بميزان كامل بأرقام محسوبة يدويًا،
  طريقتَي حساب تكلفة البضاعة، الأرصدة الدائنة غير الطبيعية، التجميع السنوي،
  ومخزون أول الشهر من رصيد آخر الشهر السابق.

> **الاختبارات لقت باج فورًا:** `cleanAccName` كانت بتشيل الأقواس **قبل** الـ trim،
> فاسم زي `"[1010] "` (مسافة زايدة من الإكسل — حاجة شائعة جدًا) كان بيفضل فيه
> `]` تايهة، والحساب مايطابقش التصنيف المحفوظ عليه. اتصلّح.

### 16. توحيد الإملاء العربي في التصنيف
التصنيف كله مبني على مطابقة أسماء الحسابات، ودليل الحسابات بيتكتب بأشكال مختلفة:
«تكلفه» و«تكلفة»، «اهلاك» و«إهلاك»، «بضاعه» و«بضاعة». أي اختلاف من دول كان بيخلّي
الحساب يفوت كل القواعد ويقع على التخمين من طبيعة الرصيد.

اتضافت `normAr()` (بتشيل التشكيل والتطويل وتوحّد الهمزات والتاء المربوطة والألف
المقصورة) و`arMatch()` اللي بتجرّب المطابقة على النص الأصلي **و** على نسخة موحّدة
من النص بنمط موحّد كمان.

**مهم في التصميم:** المطابقة الجديدة **مجموعة أكبر** من القديمة، مش مختلفة عنها —
كل اللي كان بيطابق لسه بيطابق. المكسب هو أشكال الكتابة اللي كانت بتفلت.
اتطبّقت في `guessCategory` و`equityBucketOf` ومجموعات المركز المالي وكشف الإهلاك
وتكلفة البضاعة المباعة.

### 17. المكتبات بقت مستضافة محليًا — الـ CDN اتشال خالص
React وReact-DOM وxlsx وفايربيز بقوا في `vendor/`، بتتنسخ من `node_modules` وقت البناء.
`index.html` و`sw.js` فيهم **صفر** مراجع لأي CDN.

المكسب:
- مفيش نسخة بتتغيّر من تحتينا (زي ما حصل مع بابل).
- مفيش داعي لـ SRI أصلاً — الملفات جوّه المستودع (البند ده اتقفل بالكامل).
- الأوفلاين مضمون من **أول زيارة** بدل ما يستنى وصول طرف تالت.

**التحقق (Chromium، من غير أي بدائل — الملفات الحقيقية):** زيارة أولى خزّنت 12 ملف،
وبعد **قطع الشبكة تمامًا** التطبيق أعاد التحميل وحمّل React وfirebase وXLSX من
الكاش ورندر شاشة الدخول — صفر أخطاء.

### 18. تحذير خلط طرق حساب تكلفة البضاعة
لو بعض شهور السنة استخدمت التكلفة المباشرة من الميزان وبعضها المعادلة، الإجمالي
السنوي بيبقى خليط من الطريقتين — رقم مش قابل للمقارنة. `mergeFigures` بقت بترجّع
`mixedCogsMethods` و`cogsMethodMonths`، وقائمة الدخل بتعرض تحذير بيقول كام شهر
بكل طريقة والعلاج (تثبيت الطريقة من إعدادات الشركة).

### 19. CI
`.github/workflows/ci.yml` بيشغّل الاختبارات على كل push، وبيتأكد إن `app.js`
و`vendor/` متطابقين مع المصدر — يعني بيمنع أسهل غلط ممكن يحصل في المشروع ده:
تعديل المصدر ورفعه من غير `npm run build`.

---

## خامسًا: اللي لسه مفتوح

### ١. نشر قواعد الأمان — **الوحيد الحرج**
`firestore.rules` موجود في المستودع، بس البيانات فعليًا مش محمية بيه لحد ما يتنشر:

```bash
firebase deploy --only firestore:rules --project selim-financ
```

**ده البند الوحيد المتبقّي اللي محتاج تدخّل، وهو أهم بند في المراجعة كلها.**

### ٢. قفل `email_verified` (بعد فترة)
رسايل التفعيل بتتبعت دلوقتي وفيه شريط تنبيه. لما المستخدمين الحاليين يفعّلوا
بريدهم، شيل التعليق عن السطر ده في `firestore.rules` عشان القفل يبقى على السيرفر:

```
&& request.auth.token.email_verified == true
```

### ٣. تحسينات اختيارية
- **قفل البصمة قفل واجهة محلي فقط** — موصوف صح في الكود، بس ما ينفعش يتقال عنه
  إنه حماية للبيانات. المفتاح في `localStorage` ومسح بيانات الموقع بيتخطّاه.
- **`app.src.jsx` لسه ~6,000 سطر** — دوال الحساب اتفصلت، والخطوة الجاية الطبيعية
  فصل مكوّنات التصدير (Excel/PDF) في ملف تالت.
- **توسيع الاختبارات** لتغطّي قراءة ملفات الإكسل نفسها (`buildTreeRows`,
  `buildFlatRows`) ووحدة الزكاة.

---

## ملاحظة على `firebaseConfig`
وجود `apiKey` في كود العميل **مش تسريب** — ده معرّف عام بحكم التصميم في فايربيز،
والحماية الحقيقية مصدرها قواعد Firestore وإعدادات المصادقة.
