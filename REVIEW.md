# مراجعة شاملة للتطبيق — Selim Finance

مراجعة لملف `index.html` (تطبيق صفحة واحدة، ‏~6,400 سطر، React + Firebase، يُترجَم بـ Babel في المتصفح).
النسخة وقت المراجعة: `7.32` → صارت `7.33` بعد الإصلاحات.

---

## أولاً: إصلاحات طُبِّقت فعلاً في هذا الفرع

### 1. الشركات المشتركة كانت ممكن تختفي تمامًا عن الشخص المشتَرَك معاه — (خطأ وظيفي حقيقي)
`shareAccess` كان بيخزّن البريد بحروف صغيرة (`.toLowerCase()`)، لكن الاستعلامات والتحقق من الصلاحية
كانت بتقارن بـ `user.email` كما يرجّعه فايربيز — واللي بيحتفظ بشكل البريد وقت التسجيل.
فمستخدم سجّل بـ `Ahmed@Gmail.com` ميلاقيش الشركة المشتركة معاه أبدًا، ولا رسالة خطأ ولا أي مؤشر.

**الإصلاح:** إضافة `normEmail()` و`emailVariants()`، وتوحيد المقارنة في `getRole`، وتحويل
الاستعلامين لـ `array-contains-any` بالشكلين (الموحَّد والأصلي) عشان المستندات القديمة اللي
اتخزن فيها بريد بحروف كبيرة تفضل شغالة كمان.

### 2. أرقام كانت بتتحوّل صفر بصمت عند قراءة الإكسل — (فقد بيانات)
`parseAmountCell` و`parseSignedNum` كانا بيعملا `parseFloat` بعد إزالة الفواصل فقط. النتيجة:

| الشكل في الملف | قبل | بعد |
|---|---|---|
| `(1,234.50)` سالب بالأقواس (شائع جدًا في تقارير المحاسبة) | `0` | `-1234.50` |
| `١٢٣٤٫٥٠` أرقام هندية | `0` | `1234.50` |
| `1,234-` سالب لاحق (تصدير بعض البرامج) | `0` | `-1234` |

كل حالة من دول كانت بتخرج صفر من غير تحذير، يعني ميزان يظهر متزن وهو ناقص بنود.
**الإصلاح:** دالتان جديدتان `normalizeDigits()` و`numericFromCell()` تتعاملان مع الأرقام
الهندية (العربية والفارسية)، الفاصلة العشرية العربية `٫`، فاصلة الآلاف `٬`، المسافات
غير القابلة للكسر، الأقواس، والسالب اللاحق. مُختبَرة على الحالات أعلاه.

### 3. تعقيد تربيعي في قلب حساب القوائم — (أداء)
`computeFigures` كان فيه `srcRows.find(x => x.name === name)` مستدعاة من جوّه فلاتر
بتلف هي كمان على كل الحسابات (9 مواضع). ميزان بـ 3,000 حساب = ملايين المقارنات لكل استدعاء.
**الإصلاح:** فهرس `Map` بالاسم يُبنى مرة واحدة. الدلالة محفوظة بالضبط (أول حساب لكل اسم، زي `find`).

### 4. الذاكرة المؤقتة كانت معطّلة فعليًا — (أداء)
- `sortedPeriods` كانت `[...periods].sort()` بدون `useMemo` → مصفوفة جديدة كل رندر →
  كل `useMemo` معتمدة عليها (أرقام الفترة، السنة، المركز المالي) كانت بتُعاد من الصفر.
- `HomeDashboard` كانت بتحسب 12 شهر + إجمالي السنة في كل رندر، وكل شهر بيستدعي
  `computeFigures` حتى 3 مرات → ‏**~72 مرور كامل على الميزان مع كل ضغطة زر**.

**الإصلاح:** `useMemo` للاثنين. مجتمعة مع البند (3)، دي أكبر مكسب أداء في التطبيق.

### 5. تثبيت إصدارات المكتبات الخارجية — (مخاطرة توافر)
`<script src="https://unpkg.com/@babel/standalone/babel.min.js">` بدون إصدار.
‏`@babel/standalone` وصل فعلاً للإصدار **8.0.4** (نقلة رئيسية)، يعني كل المستخدمين
انتقلوا لإصدار كبير جديد من غير أي اختبار. اختبرت مصدر التطبيق على بابل 8 وهو **بيترجم
تمام حاليًا** — يعني ما فيش عطل قائم دلوقتي، لكن التطبيق كان معلّق على الحظ:
أي تغيير كاسر في بابل بيوقّف التطبيق كله لكل المستخدمين في نفس اللحظة.

**الإصلاح:** تثبيت `@babel/standalone@7.29.8`، و`react`/`react-dom` على `18.3.1` بدل `@18`.

**التحقق:** مصدر التطبيق كامل يُترجَم ويُحلَّل بنجاح بالإصدار المثبّت (اختُبر ببابل 7.29.8 و8.0.4).

---

## ثانيًا: ملاحظات لم تُعالَج — مرتبة بالأولوية

### أ. حرجة

**1. قواعد أمان Firestore غير موجودة في المستودع — ولا يمكن التحقق منها من هنا.**
كل التحكم في الصلاحيات في التطبيق ده على جهة العميل: `getRole()` و`readOnly` بيتحكموا
في شكل الواجهة فقط. مفيش ملف `firestore.rules` في المستودع، يعني ما ينفعش أراجعه.
لو القواعد المنشورة متساهلة، أي مستخدم مسجَّل يقدر يقرأ ويكتب أي شركة عن طريق الـ SDK مباشرة —
والبيانات دي قوائم مالية وزكاة. **ده أهم بند في المراجعة كلها.**
المطلوب: إضافة `firestore.rules` للمستودع، بحيث يسمح بالقراءة فقط للمالك أو من بريده
في `editorEmails`/`viewerEmails`، والكتابة للمالك والمحرّرين فقط.

**2. لا يوجد Service Worker — التطبيق مش شغال أوفلاين رغم إنه PWA.**
فيه `manifest.json` وأيقونات، لكن مفيش أي `navigator.serviceWorker.register`.
ودالة `forceUpdate()` بتمسح `caches` — وهي فاضية أصلاً لأن مفيش SW بيملاها.

### ب. مهمة

**3. الأسماء المتكررة بتلخبط التصنيف.** كل البحث والتجميع بيتم بمفتاح اسم الحساب
(`byName`, `ovOf`, `mergeRows` في `mergeFigures`). ميزان فيه حسابين بنفس الاسم في فرعين
مختلفين → الاتنين بياخدوا تصنيف الأول، والأرقام بتتجمع في بند واحد. الحل الصحيح:
المفتاح يبقى كود الحساب أو معرّف فريد، مش الاسم.

**4. ابتلاع الأخطاء بشكل واسع.** `.catch(() => {})` منتشرة على عمليات حفظ حقيقية
(`update`, `set`, `batch`). لو الحفظ فشل — صلاحية، شبكة، أو حد الدفعة — المستخدم
بيشوف واجهة طبيعية ويفتكر إن البيانات اتحفظت. لازم على الأقل مؤشر "لم يتم الحفظ".

**5. `periodCount` بيتكتب من كل من يفتح الشركة**، بما فيهم المشاهدون اللي (المفروض)
ما عندهمش صلاحية كتابة → أخطاء صلاحية متكررة مبتولعة بصمت. الأنسب: المالك/المحرّر فقط.

**6. `uid()` مبني على `Math.random()`** ومستخدم كمعرّف مستندات في Firestore.
‏8 محارف من `Math.random` مش مضمونة التفرّد ولا مقاومة التصادم. الأنسب `crypto.randomUUID()`.

### ج. متوسطة

**7. ترجمة بابل في المتصفح.** ‏~490KB مصدر بيتحوّل لـ ~570KB كود في كل فتحة للتطبيق،
على خيط الواجهة الرئيسي. ده أبطأ حاجة في بدء التشغيل على الموبايل. الحل الطبيعي:
خطوة بناء بسيطة تُخرِج JS جاهز، وتشيل `@babel/standalone` (وهو أكبر ملف بيتحمّل) خالص.

**8. مفيش SRI على سكربتات الـ CDN.** بعد التثبيت على إصدارات محددة، إضافة `integrity`
تقفل الباب على أي تعديل من جهة الـ CDN. (محتاجة حساب الهاشات وقت وجود شبكة.)

**9. `mergeFigures` بتخلط طرق حساب تكلفة البضاعة.** لو أي شهر في السنة استخدم
الطريقة المباشرة، بتجمع تكلفة كل الشهور — بما فيها اللي اتحسبت بالمعادلة. `cogsMethod`
المثبّت على مستوى الشركة بيخفف ده، بس الأفضل تحذير صريح لما الشهور تختلف.

**10. التصنيف كله بتعبيرات نمطية على أسماء عربية.** مفيش توحيد للهمزات والتاء المربوطة
بشكل عام (فيه معالجة موضعية لـ `تأمين|تامين` بس مش في كل مكان). أي اختلاف إملائي
في دليل الحسابات بيغيّر تصنيف الحساب — وده يغيّر أرقام القوائم.

**11. قفل البصمة قفل واجهة محلي فقط.** وده موصوف صح في تعليق الكود، بس يستحق التوضيح:
المفتاح في `localStorage`، ومسح بيانات الموقع بيتخطّاه. جلسة فايربيز هي الأمان الحقيقي.
تمام كتجربة استخدام — بس ما ينفعش يتقال عنه إنه حماية للبيانات.

### د. هيكلية

**12. ملف واحد 6,400 سطر، بدون اختبارات، بدون CI، بدون خطوة بناء.**
لتطبيق بيحسب قوائم مالية وزكاة، غياب الاختبارات على `computeFigures` و`mergeFigures`
و`parseAmountCell` هو أكبر خطر طويل المدى. أقل خطوة مفيدة: فصل دوال الحساب في ملف
مستقل + مجموعة اختبارات على موازين حقيقية معروفة النتيجة.

**13. مفيش README ولا LICENSE** ولا أي توثيق لنموذج البيانات في Firestore.

---

## ملاحظة على `firebaseConfig`
وجود `apiKey` في كود العميل **مش تسريب** — ده معرّف عام بحكم التصميم في فايربيز،
والحماية الحقيقية مصدرها قواعد Firestore وإعدادات المصادقة. لكن ده بيرجّعنا للبند (أ-1):
طالما القواعد هي خط الدفاع الوحيد، لازم تكون موجودة ومراجَعة ومحفوظة في المستودع.
